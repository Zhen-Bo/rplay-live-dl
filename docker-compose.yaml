services:
  rplay-live-dl:
    image: paverz/rplay-live-dl:v2.1.1-vibe
    container_name: rplay-live-dl
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Taipei
    volumes:
      - ./config:/app/config
      - ./archive:/app/archive
      - ./logs:/app/logs
    restart: unless-stopped
